[tool.poetry]
name = "poetry-project"
version = "0.2.0"
description = ""
authors = ["zeeshan <through.learning@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
